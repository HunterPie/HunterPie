<Grid x:Class="HunterPie.UI.Controls.Effects.CursorHighLight"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:HunterPie.UI.Controls.Effects"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      d:DesignHeight="450"
      d:DesignWidth="800"
      MouseMove="OnMouseMove"
      mc:Ignorable="d">
	<Grid.Resources>
		<BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
	</Grid.Resources>
	<ContentControl Content="{Binding Children, RelativeSource={RelativeSource AncestorType={x:Type local:CursorHighLight}}}" />
	<Canvas Background="Transparent"
	        ClipToBounds="True"
	        IsHitTestVisible="False">
		<Rectangle x:Name="PART_Highlight"
		           Width="500"
		           Height="500"
		           Fill="{DynamicResource Brushes.HunterPie.Highlight}"
		           Opacity="0"
		           Visibility="{Binding IsHighlightEnabled, Mode=OneWay, RelativeSource={RelativeSource AncestorType={x:Type local:CursorHighLight}}, Converter={StaticResource BooleanToVisibilityConverter}}">
			<Rectangle.Style>
				<Style TargetType="{x:Type Rectangle}">
					<Style.Triggers>
						<DataTrigger Binding="{Binding IsMouseOver, Mode=OneWay, RelativeSource={RelativeSource AncestorType={x:Type local:CursorHighLight}}}"
						             Value="True">
							<DataTrigger.EnterActions>
								<BeginStoryboard>
									<Storyboard Storyboard.TargetProperty="Opacity">
										<DoubleAnimation To="0.2"
										                 Duration="0:0:0.3" />
									</Storyboard>
								</BeginStoryboard>
							</DataTrigger.EnterActions>
							<DataTrigger.ExitActions>
								<BeginStoryboard>
									<Storyboard Storyboard.TargetProperty="Opacity">
										<DoubleAnimation To="0"
										                 Duration="0:0:0.3" />
									</Storyboard>
								</BeginStoryboard>
							</DataTrigger.ExitActions>
						</DataTrigger>
					</Style.Triggers>
				</Style>
			</Rectangle.Style>
		</Rectangle>
	</Canvas>
</Grid>
