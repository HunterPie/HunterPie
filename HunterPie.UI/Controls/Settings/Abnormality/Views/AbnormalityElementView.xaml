<architecture:ClickableControl x:Class="HunterPie.UI.Controls.Settings.Abnormality.Views.AbnormalityElementView"
                               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                               xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                               xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                               xmlns:local="clr-namespace:HunterPie.UI.Controls.Settings.Abnormality.Views"
                               xmlns:viewModels="clr-namespace:HunterPie.UI.Controls.Settings.Abnormality.ViewModels"
                               xmlns:converters="clr-namespace:HunterPie.UI.Architecture.Converters"
                               xmlns:buttons="clr-namespace:HunterPie.UI.Controls.Buttons"
                               xmlns:architecture="clr-namespace:HunterPie.UI.Architecture"
                               xmlns:image="clr-namespace:HunterPie.UI.Controls.Image"
                               mc:Ignorable="d"
                               MinHeight="40"
                               d:DesignWidth="200"
                               d:DataContext="{d:DesignInstance viewModels:AbnormalityElementViewModel}">
    <UserControl.Resources>
        <converters:AbnormalityStringToIconConverter x:Key="AbnormalityStringToIconConverter"/>
    </UserControl.Resources>
    <buttons:Button Padding="0"
                    HorizontalContentAlignment="Stretch"
                    VerticalContentAlignment="Stretch"
                    CornerRadius="5"
                    BorderThickness="1"
                    Background="{DynamicResource Brushes.HunterPie.Background.Primary}"
                    BorderBrush="{DynamicResource Brushes.HunterPie.Border}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="25"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="1.5*"/>
            </Grid.RowDefinitions>

            <Border Grid.Column="0"
                    Grid.Row="0"
                    Grid.RowSpan="2"
                    Background="{DynamicResource Brushes.HunterPie.Background.Secondary}"
                    BorderThickness="0,0,1,0"
                    BorderBrush="{DynamicResource Brushes.HunterPie.Border}"/>
            <Image Grid.Column="0"
                   Grid.Row="0"
                   Grid.RowSpan="2"
                   Margin="10"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"
                   Source="{Binding Icon, Mode=OneWay, Converter={StaticResource AbnormalityStringToIconConverter}}"
                   d:Source="{StaticResource ICON_DANGOBOOSTER}"/>

            <!-- Text -->
            <TextBlock Grid.Column="1"
                       Grid.Row="0"
                       d:Text="Abnormality"
                       Text="{Binding Name, Mode=OneWay}"
                       VerticalAlignment="Bottom"
                       Margin="2,0,0,0"
                       FontSize="13"
                       TextTrimming="CharacterEllipsis"
                       Foreground="{DynamicResource Brushes.HunterPie.Foreground.Primary}"/>
            <TextBlock Grid.Column="1"
                       Grid.Row="1"
                       d:Text="Category"
                       Text="{Binding Category, Mode=OneWay}"
                       FontWeight="Regular"
                       VerticalAlignment="Top"
                       Margin="2,0,0,0"
                       Foreground="{DynamicResource Brushes.HunterPie.Foreground.Tertiary}"
                       FontSize="11"/>

            <image:Icon Grid.Row="0"
                        Grid.RowSpan="2"
                        Grid.Column="2"
                        Width="20"
                        Height="20"
                        Margin="0,0,5,0"
                        Image="{DynamicResource ICON_CHECKMARK}"
                        d:Foreground="{DynamicResource Brushes.HunterPie.Foreground.Tertiary}">
                <image:Icon.Style>
                    <Style TargetType="{x:Type image:Icon}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType={x:Type local:AbnormalityElementView}}}"
                                         Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource Brushes.HunterPie.Success}"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType={x:Type local:AbnormalityElementView}}}"
                                         Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource Brushes.HunterPie.Foreground.Tertiary}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </image:Icon.Style>
            </image:Icon>
        </Grid>
    </buttons:Button>
</architecture:ClickableControl>
