<UserControl x:Class="HunterPie.UI.Controls.Buttons.TabItem"
             x:Name="TabItemControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HunterPie.UI.Controls.Buttons"
             xmlns:markup="clr-namespace:HunterPie.UI.Architecture.Markup"
             mc:Ignorable="d" 
             d:DesignHeight="30" 
             d:DesignWidth="120"
             ClipToBounds="True"
             ToolTip="{Binding Description, ElementName=TabItemControl}"
             Cursor="Hand">
    <UserControl.Resources>
        <Storyboard x:Key="RippleAnimation" 
                    Storyboard.TargetName="PART_Ripple">
            <DoubleAnimation Storyboard.TargetProperty="Width" 
                             From="0"
                             Duration="0:0:0.3" />
            <ThicknessAnimation Storyboard.TargetProperty="Margin" 
                                Duration="0:0:0.3" />
            <DoubleAnimation Duration="0:0:0.8"
                             Storyboard.TargetProperty="Opacity"
                             From="1" 
                             To="0" />
            <DoubleAnimation Storyboard.TargetProperty="Width" 
                             To="0"
                             BeginTime="0:0:1.25" 
                             Duration="0:0:0" />
            <DoubleAnimation BeginTime="0:0:1.25" 
                             Duration="0:0:0"
                             Storyboard.TargetProperty="Opacity"
                             To="1" />
        </Storyboard>
    </UserControl.Resources>
    <local:Button BorderBrush="{DynamicResource Brushes.HunterPie.Border}"
                  BorderThickness="1"
                  Background="{DynamicResource Brushes.HunterPie.Background.Primary}"
                  CornerRadius="5"
                  Padding="0"
                  HorizontalContentAlignment="Stretch"
                  VerticalContentAlignment="Stretch"
                  Click="OnClick">
        <Grid>
            <Border CornerRadius="5">
                <Border.Style>
                    <Style TargetType="Border">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType={x:Type TabItem}}}"
                                         Value="True">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush EndPoint="1,1" 
                                                             StartPoint="0,.5" >
                                            <GradientStop Color="#00000000"/>
                                            <GradientStop Color="{markup:BrushColor Brush={StaticResource Brushes.HunterPie.Accent.Solid}, Opacity=0.1}" 
                                                          Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
            </Border>

            <Grid Margin="10,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <Rectangle Grid.Column="0" 
                           HorizontalAlignment="Stretch"
                           VerticalAlignment="Stretch"
                           Width="20"
                           Height="{Binding Width, RelativeSource={RelativeSource Self}}"
                           Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type local:TabItem}}}">
                    <Rectangle.OpacityMask>
                        <ImageBrush d:ImageSource="{StaticResource ICON_DISCORD}"
                                    ImageSource="{Binding Icon, RelativeSource={RelativeSource AncestorType={x:Type local:TabItem}}}"
                                    TileMode="Tile" 
                                    Stretch="Uniform"/>
                    </Rectangle.OpacityMask>
                </Rectangle>

                <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType={x:Type local:TabItem}}}"
                           Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type local:TabItem}}}"
                           Grid.Column="1"
                           Margin="10,0,0,0"
                           FontWeight="Medium"
                           VerticalAlignment="Center"/>
            </Grid>
        </Grid>
    </local:Button>
</UserControl>
