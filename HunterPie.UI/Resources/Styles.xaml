<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:buttons="clr-namespace:HunterPie.UI.Controls.Buttons;assembly=HunterPie.UI"
                    xmlns:converters="clr-namespace:HunterPie.UI.Architecture.Converters;assembly=HunterPie.UI"
                    xmlns:effects="clr-namespace:HunterPie.UI.Controls.Effects;assembly=HunterPie.UI"
                    xmlns:label="clr-namespace:HunterPie.UI.Controls.Label;assembly=HunterPie.UI"
                    xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
                    xmlns:markup="clr-namespace:HunterPie.UI.Architecture.Markup;assembly=HunterPie.UI"
                    xmlns:progress="clr-namespace:HunterPie.UI.Overlay.Controls.Progress;assembly=HunterPie.UI"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">
	<!--  Converters  -->
	<converters:EnumToStringConverter x:Key="EnumToStringConverter" />

	<!--  Font  -->
	<FontFamily x:Key="Font.Default">pack://application:,,,/HunterPie.UI;component/Assets/Fonts/#Work Sans, Segoe UI, Microsoft Sans Serif</FontFamily>

	<!--  Styles  -->
	<Style x:Key="{x:Type ToolTip}"
	       TargetType="{x:Type ToolTip}">
		<Style.Resources>
			<Style TargetType="{x:Type ContentPresenter}">
				<Style.Resources>
					<Style TargetType="{x:Type TextBlock}">
						<Setter Property="TextWrapping" Value="WrapWithOverflow" />
					</Style>
				</Style.Resources>
			</Style>
		</Style.Resources>
		<Setter Property="FontFamily" Value="{DynamicResource Font.Default}" />
		<Setter Property="FontSize" Value="14" />
		<Setter Property="Background" Value="{DynamicResource Brushes.HunterPie.Background.Primary}" />
		<Setter Property="Foreground" Value="{DynamicResource Brushes.HunterPie.Foreground.Primary}" />
		<Setter Property="BorderBrush" Value="{DynamicResource Brushes.HunterPie.Border}" />
		<Setter Property="BorderThickness" Value="1" />
		<Setter Property="MaxWidth" Value="350" />
		<Setter Property="ToolTipService.InitialShowDelay" Value="1000" />
		<Setter Property="ToolTipService.BetweenShowDelay" Value="0" />
		<Style.Triggers>
			<Trigger Property="IsVisible" Value="True">
				<Trigger.EnterActions>
					<BeginStoryboard>
						<Storyboard>
							<DoubleAnimation Storyboard.TargetProperty="Opacity"
							                 From="0"
							                 To="1"
							                 Duration="0:0:0.25" />
							<DoubleAnimation Storyboard.TargetProperty="VerticalOffset"
							                 From="-35"
							                 Duration="0:0:0.25">
								<DoubleAnimation.EasingFunction>
									<QuadraticEase />
								</DoubleAnimation.EasingFunction>
							</DoubleAnimation>
						</Storyboard>
					</BeginStoryboard>
				</Trigger.EnterActions>
			</Trigger>
		</Style.Triggers>
	</Style>

	<Style x:Key="ScrollBarLineButton"
	       TargetType="{x:Type RepeatButton}">
		<Setter Property="Visibility" Value="Hidden" />
	</Style>
	<Style x:Key="ScrollBarPageButton"
	       TargetType="{x:Type RepeatButton}">
		<Setter Property="Visibility" Value="Hidden" />
	</Style>

	<Style x:Key="ScrollBarThumb"
	       TargetType="{x:Type Thumb}">
		<Setter Property="SnapsToDevicePixels" Value="True" />
		<Setter Property="OverridesDefaultStyle" Value="true" />
		<Setter Property="IsTabStop" Value="false" />
		<Setter Property="Focusable" Value="false" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type Thumb}">
					<Border BorderBrush="{TemplateBinding BorderBrush}"
					        BorderThickness="0"
					        Background="{DynamicResource Brushes.HunterPie.Foreground.Tertiary}"
					        CornerRadius="2" />
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<ControlTemplate x:Key="VerticalScrollBar"
	                 TargetType="{x:Type ScrollBar}">
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition MaxHeight="0" />
				<RowDefinition Height="0.00001*" />
				<RowDefinition MaxHeight="0" />
			</Grid.RowDefinitions>
			<Border Grid.Row="0"
			        Grid.RowSpan="3"
			        Background="{DynamicResource Brushes.HunterPie.Background.Tertiary}"
			        CornerRadius="2" />
			<RepeatButton Grid.Row="0"
			              Height="18"
			              Command="ScrollBar.LineUpCommand"
			              Content="M 0 4 L 8 4 L 4 0 Z"
			              Style="{StaticResource ScrollBarLineButton}" />
			<Track Name="PART_Track"
			       Grid.Row="1"
			       IsDirectionReversed="true">
				<Track.DecreaseRepeatButton>
					<RepeatButton Command="ScrollBar.PageUpCommand"
					              Style="{StaticResource ScrollBarPageButton}" />
				</Track.DecreaseRepeatButton>
				<Track.Thumb>
					<Thumb BorderBrush="{DynamicResource Brushes.HunterPie.Foreground.Tertiary}"
					       Margin="1,1,1,1"
					       Background="{DynamicResource Brushes.HunterPie.Foreground.Tertiary}"
					       Style="{StaticResource ScrollBarThumb}" />
				</Track.Thumb>
				<Track.IncreaseRepeatButton>
					<RepeatButton Command="ScrollBar.PageDownCommand"
					              Style="{StaticResource ScrollBarPageButton}" />
				</Track.IncreaseRepeatButton>
			</Track>
			<RepeatButton Grid.Row="2"
			              Height="18"
			              Command="ScrollBar.LineDownCommand"
			              Content="M 0 0 L 4 4 L 8 0 Z"
			              Style="{StaticResource ScrollBarLineButton}" />
		</Grid>
	</ControlTemplate>
	<ControlTemplate x:Key="HorizontalScrollBar"
	                 TargetType="{x:Type ScrollBar}">
		<Grid>
			<Grid.ColumnDefinitions>
				<ColumnDefinition MaxWidth="18" />
				<ColumnDefinition Width="0.00001*" />
				<ColumnDefinition MaxWidth="18" />
			</Grid.ColumnDefinitions>
			<Border Grid.Column="0"
			        Grid.ColumnSpan="3"
			        Background="{DynamicResource Brushes.HunterPie.Background.Tertiary}"
			        CornerRadius="2" />
			<RepeatButton Grid.Column="0"
			              Width="0"
			              Command="ScrollBar.LineLeftCommand"
			              Content="M 4 0 L 4 8 L 0 4 Z"
			              Style="{StaticResource ScrollBarLineButton}" />
			<Track Name="PART_Track"
			       Grid.Column="1"
			       IsDirectionReversed="False">
				<Track.DecreaseRepeatButton>
					<RepeatButton Command="ScrollBar.PageLeftCommand"
					              Style="{StaticResource ScrollBarPageButton}" />
				</Track.DecreaseRepeatButton>
				<Track.Thumb>
					<Thumb BorderBrush="{DynamicResource Brushes.HunterPie.Foreground.Tertiary}"
					       Margin="1"
					       Background="{DynamicResource Brushes.HunterPie.Foreground.Tertiary}"
					       Style="{StaticResource ScrollBarThumb}" />
				</Track.Thumb>
				<Track.IncreaseRepeatButton>
					<RepeatButton Command="ScrollBar.PageRightCommand"
					              Style="{StaticResource ScrollBarPageButton}" />
				</Track.IncreaseRepeatButton>
			</Track>
			<RepeatButton Grid.Column="2"
			              Width="18"
			              Command="ScrollBar.LineRightCommand"
			              Content="M 0 0 L 4 4 L 0 8 Z"
			              Style="{StaticResource ScrollBarLineButton}" />
		</Grid>
	</ControlTemplate>
	<Style x:Key="{x:Type ScrollBar}"
	       TargetType="{x:Type ScrollBar}">
		<Setter Property="SnapsToDevicePixels" Value="True" />
		<Setter Property="OverridesDefaultStyle" Value="true" />
		<Style.Triggers>
			<Trigger Property="Orientation" Value="Horizontal">
				<Setter Property="Width" Value="Auto" />
				<Setter Property="Height" Value="5" />
				<Setter Property="Template" Value="{StaticResource HorizontalScrollBar}" />
			</Trigger>
			<Trigger Property="Orientation" Value="Vertical">
				<Setter Property="Width" Value="5" />
				<Setter Property="Height" Value="Auto" />
				<Setter Property="Template" Value="{StaticResource VerticalScrollBar}" />
			</Trigger>

		</Style.Triggers>
	</Style>

	<Style x:Key="ScrollViewerStyle"
	       TargetType="{x:Type ScrollViewer}">
		<Setter Property="OverridesDefaultStyle" Value="True" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type ScrollViewer}">
					<Grid>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="Auto" />
							<ColumnDefinition />
						</Grid.ColumnDefinitions>
						<Grid.RowDefinitions>
							<RowDefinition Height="Auto" />
							<RowDefinition />
						</Grid.RowDefinitions>
						<ScrollContentPresenter Grid.Row="0"
						                        Grid.Column="0" />
						<ScrollBar Name="PART_VerticalScrollBar"
						           Grid.Row="0"
						           Grid.Column="0"
						           Maximum="{TemplateBinding ScrollableHeight}"
						           ViewportSize="{TemplateBinding ViewportHeight}"
						           Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
						           Value="{TemplateBinding VerticalOffset}" />
						<ScrollBar Name="PART_HorizontalScrollBar"
						           Grid.Row="1"
						           Grid.Column="1"
						           Maximum="{TemplateBinding ScrollableWidth}"
						           Orientation="Horizontal"
						           ViewportSize="{TemplateBinding ViewportWidth}"
						           Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"
						           Value="{TemplateBinding HorizontalOffset}" />
					</Grid>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<!--  ComboBox style  -->
	<ControlTemplate x:Key="ComboBoxToggleButtonTemplate"
	                 TargetType="ToggleButton">
		<Grid>
			<Grid.ColumnDefinitions>
				<ColumnDefinition />
				<ColumnDefinition Width="40" />
			</Grid.ColumnDefinitions>
			<Border Name="Border"
			        Grid.Column="0"
			        Grid.ColumnSpan="2"
			        BorderBrush="{DynamicResource Brushes.HunterPie.Border}"
			        BorderThickness="1"
			        Background="{DynamicResource Brushes.HunterPie.Background.Primary}"
			        CornerRadius="5" />
			<Border Name="ButtonBorder"
			        Grid.Column="1"
			        BorderBrush="{DynamicResource Brushes.HunterPie.Border}"
			        BorderThickness="1,0,0,0"
			        Margin="1,1,1,1"
			        Background="{DynamicResource Brushes.HunterPie.Background.Secondary}"
			        CornerRadius="0,5,5,0" />

			<Path Name="Arrow"
			      Grid.Column="1"
			      HorizontalAlignment="Center"
			      VerticalAlignment="Center"
			      Data="M0,0 L0,2 L4,6 L8,2 L8,0 L4,4 z"
			      Fill="{DynamicResource Brushes.HunterPie.Accent}" />
		</Grid>
		<ControlTemplate.Triggers>
			<Trigger Property="ToggleButton.IsChecked" Value="True">
				<Setter TargetName="ButtonBorder" Property="Panel.Background" Value="{DynamicResource Brushes.HunterPie.Background.Tertiary}" />
				<Setter TargetName="Arrow" Property="Shape.Fill" Value="{DynamicResource Brushes.HunterPie.Foreground.Tertiary}" />
			</Trigger>
		</ControlTemplate.Triggers>
	</ControlTemplate>

	<Style x:Key="{x:Type ListBoxItem}"
	       TargetType="{x:Type ListBoxItem}">
		<Setter Property="BorderThickness" Value="0" />
		<Setter Property="Cursor" Value="Hand" />
		<Setter Property="Foreground" Value="{DynamicResource White@100}" />
		<Style.Triggers>
			<Trigger Property="IsMouseOver" Value="True">
				<Setter Property="Background" Value="{DynamicResource Gray@400}" />
			</Trigger>
			<Trigger Property="IsSelected" Value="True">
				<Setter Property="Background" Value="{DynamicResource Gray@400}" />
				<Setter Property="Foreground" Value="{DynamicResource Brushes.HunterPie.Accent}" />
			</Trigger>
		</Style.Triggers>
	</Style>

	<Style x:Key="{x:Type ComboBox}"
	       TargetType="{x:Type ComboBox}">
		<Setter Property="FontFamily" Value="{DynamicResource Font.Default}" />
		<Setter Property="FontSize" Value="14" />
		<Setter Property="Cursor" Value="Hand" />
		<Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
		<Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
		<Setter Property="ScrollViewer.CanContentScroll" Value="True" />
		<Setter Property="TextElement.Foreground" Value="{DynamicResource Brushes.HunterPie.Foreground.Primary}" />
		<Setter Property="ItemContainerStyle">
			<Setter.Value>
				<Style TargetType="ComboBoxItem">
					<Setter Property="Template">
						<Setter.Value>
							<ControlTemplate TargetType="ComboBoxItem">
								<effects:CursorHighLight>
									<ContentPresenter Margin="2,5" />
								</effects:CursorHighLight>
							</ControlTemplate>
						</Setter.Value>
					</Setter>
					<Setter Property="BorderThickness" Value="0" />
					<Style.Triggers>
						<Trigger Property="IsSelected" Value="True">
							<Setter Property="Foreground" Value="{DynamicResource Brushes.HunterPie.Accent}" />
						</Trigger>
					</Style.Triggers>
				</Style>
			</Setter.Value>
		</Setter>
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="ComboBox">
					<Grid>
						<ToggleButton Name="ToggleButton"
						              Grid.Column="0"
						              ClickMode="Press"
						              Focusable="False"
						              IsChecked="{Binding Path=IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
						              Template="{StaticResource ComboBoxToggleButtonTemplate}" />
						<ContentPresenter Name="ContentSite"
						                  Margin="5,3,40,3"
						                  HorizontalAlignment="Left"
						                  VerticalAlignment="Center"
						                  Content="{TemplateBinding ComboBox.SelectionBoxItem}"
						                  ContentTemplate="{TemplateBinding ComboBox.SelectionBoxItemTemplate}"
						                  ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
						                  IsHitTestVisible="False" />
						<TextBox Name="PART_EditableTextBox"
						         Margin="3,3,23,3"
						         HorizontalAlignment="Left"
						         VerticalAlignment="Center"
						         Background="Transparent"
						         Focusable="True"
						         IsReadOnly="{TemplateBinding IsReadOnly}"
						         Visibility="Hidden">
							<TextBox.Template>
								<ControlTemplate TargetType="TextBox">
									<Border Name="PART_ContentHost"
									        Focusable="False" />
								</ControlTemplate>
							</TextBox.Template>
						</TextBox>
						<!--  Popup showing items  -->
						<Popup Name="Popup"
						       AllowsTransparency="True"
						       Focusable="False"
						       IsOpen="{TemplateBinding ComboBox.IsDropDownOpen}"
						       Placement="Bottom"
						       PopupAnimation="Fade">
							<Grid Name="DropDown"
							      MinWidth="{TemplateBinding FrameworkElement.ActualWidth}"
							      MaxHeight="{TemplateBinding ComboBox.MaxDropDownHeight}"
							      SnapsToDevicePixels="True">
								<Border Name="DropDownBorder"
								        BorderBrush="{DynamicResource Brushes.HunterPie.Border}"
								        BorderThickness="1,1,1,1"
								        Margin="0,0,0,0"
								        Background="{DynamicResource Brushes.HunterPie.Background.Secondary}"
								        CornerRadius="5" />
								<ScrollViewer Margin="0"
								              SnapsToDevicePixels="True">
									<ItemsPresenter Cursor="Hand"
									                KeyboardNavigation.DirectionalNavigation="Contained" />
								</ScrollViewer>
							</Grid>
						</Popup>
					</Grid>
					<ControlTemplate.Triggers>
						<Trigger Property="ItemsControl.HasItems" Value="False">
							<Setter TargetName="DropDownBorder" Property="FrameworkElement.MinHeight" Value="95" />
						</Trigger>
						<Trigger Property="ItemsControl.IsGrouping" Value="True">
							<Setter Property="ScrollViewer.CanContentScroll" Value="False" />
						</Trigger>
						<Trigger Property="ComboBox.IsEditable" Value="True">
							<Setter Property="KeyboardNavigation.IsTabStop" Value="False" />
							<Setter TargetName="PART_EditableTextBox" Property="UIElement.Visibility" Value="Visible" />
							<Setter TargetName="ContentSite" Property="UIElement.Visibility" Value="Hidden" />
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<!--  TextBox  -->
	<Style x:Key="{x:Type TextBox}"
	       TargetType="{x:Type TextBox}">
		<Setter Property="CaretBrush" Value="{DynamicResource Brushes.HunterPie.Accent}" />
		<Setter Property="Foreground" Value="{DynamicResource Brushes.HunterPie.Foreground.Primary}" />
		<Setter Property="FontFamily" Value="{DynamicResource Font.Default}" />
		<Setter Property="VerticalContentAlignment" Value="Center" />
		<Setter Property="FontSize" Value="14" />
		<Setter Property="Padding" Value="2" />
		<Setter Property="OverridesDefaultStyle" Value="True" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="TextBox">
					<Border BorderBrush="{DynamicResource Brushes.HunterPie.Border}"
					        BorderThickness="1"
					        Background="{DynamicResource Brushes.HunterPie.Background.Primary}"
					        ClipToBounds="True"
					        CornerRadius="5">
						<ScrollViewer x:Name="PART_ContentHost" />
					</Border>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
		<Setter Property="Validation.ErrorTemplate">
			<Setter.Value>
				<ControlTemplate>
					<Border BorderBrush="{DynamicResource Brushes.HunterPie.Error}"
					        BorderThickness="1"
					        CornerRadius="5">
						<Border.Effect>
							<DropShadowEffect BlurRadius="10"
							                  RenderingBias="Performance"
							                  ShadowDepth="0"
							                  Color="{markup:BrushColor Brush={StaticResource Brushes.HunterPie.Error}}" />
						</Border.Effect>
						<AdornedElementPlaceholder />
					</Border>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<!--  PasswordBox  -->
	<Style x:Key="{x:Type PasswordBox}"
	       TargetType="{x:Type PasswordBox}">
		<Setter Property="CaretBrush" Value="{DynamicResource Brushes.HunterPie.Accent}" />
		<Setter Property="Foreground" Value="{DynamicResource Brushes.HunterPie.Foreground.Primary}" />
		<Setter Property="FontFamily" Value="{DynamicResource Font.Default}" />
		<Setter Property="FontSize" Value="14" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="PasswordBox">
					<Border BorderBrush="{DynamicResource Brushes.HunterPie.Border}"
					        BorderThickness="1"
					        Background="{DynamicResource Brushes.HunterPie.Background.Primary}"
					        CornerRadius="5">
						<ScrollViewer x:Name="PART_ContentHost"
						              Background="Transparent"
						              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
					</Border>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<!--  Highlight  -->
	<Style x:Key="StyleHighlight"
	       TargetType="{x:Type Rectangle}">
		<Style.Triggers>
			<DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}}"
			             Value="True">
				<DataTrigger.EnterActions>
					<BeginStoryboard>
						<Storyboard Storyboard.TargetProperty="Opacity">
							<DoubleAnimation To="0.2"
							                 Duration="0:0:0.3" />
						</Storyboard>
					</BeginStoryboard>
				</DataTrigger.EnterActions>
				<DataTrigger.ExitActions>
					<BeginStoryboard>
						<Storyboard Storyboard.TargetProperty="Opacity">
							<DoubleAnimation To="0"
							                 Duration="0:0:0.3" />
						</Storyboard>
					</BeginStoryboard>
				</DataTrigger.ExitActions>
			</DataTrigger>
		</Style.Triggers>
	</Style>

	<!--  Slider  -->
	<Style x:Key="SliderProgressRightSide"
	       TargetType="RepeatButton">
		<Setter Property="SnapsToDevicePixels" Value="true" />
		<Setter Property="OverridesDefaultStyle" Value="true" />
		<Setter Property="IsTabStop" Value="false" />
		<Setter Property="Focusable" Value="false" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="RepeatButton">
					<Border BorderBrush="{DynamicResource Brushes.HunterPie.Border}"
					        BorderThickness="1"
					        Height="2"
					        CornerRadius="1" />
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style x:Key="SliderProgressLeftSide"
	       TargetType="RepeatButton">
		<Setter Property="SnapsToDevicePixels" Value="true" />
		<Setter Property="OverridesDefaultStyle" Value="true" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="RepeatButton">
					<Border BorderBrush="{DynamicResource Brushes.HunterPie.Accent}"
					        BorderThickness="1"
					        Height="2"
					        CornerRadius="1" />
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style x:Key="SliderThumb"
	       TargetType="Thumb">
		<Setter Property="SnapsToDevicePixels" Value="true" />
		<Setter Property="OverridesDefaultStyle" Value="true" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="Thumb">
					<Ellipse Stroke="{DynamicResource Brushes.HunterPie.Foreground.Primary}"
					         StrokeThickness="1"
					         Width="13"
					         Height="13"
					         Cursor="Hand"
					         Fill="{DynamicResource Brushes.HunterPie.Foreground.Primary}" />
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<ControlTemplate x:Key="Slider"
	                 TargetType="Slider">
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto"
				               MinHeight="{TemplateBinding MinHeight}" />
				<RowDefinition Height="Auto" />
			</Grid.RowDefinitions>
			<Track x:Name="PART_Track"
			       Grid.Row="1">
				<Track.DecreaseRepeatButton>
					<RepeatButton Command="Slider.DecreaseLarge"
					              Style="{StaticResource SliderProgressLeftSide}" />
				</Track.DecreaseRepeatButton>
				<Track.Thumb>
					<Thumb Style="{StaticResource SliderThumb}" />
				</Track.Thumb>
				<Track.IncreaseRepeatButton>
					<RepeatButton Command="Slider.IncreaseLarge"
					              Style="{StaticResource SliderProgressRightSide}" />
				</Track.IncreaseRepeatButton>
			</Track>
		</Grid>
	</ControlTemplate>

	<Style x:Key="SliderDefault"
	       TargetType="Slider">
		<Setter Property="Focusable" Value="False" />
		<Setter Property="SnapsToDevicePixels" Value="true" />
		<Setter Property="OverridesDefaultStyle" Value="true" />
		<Style.Triggers>
			<Trigger Property="Orientation" Value="Horizontal">
				<Setter Property="MinHeight" Value="21" />
				<Setter Property="MinWidth" Value="104" />
				<Setter Property="Template" Value="{StaticResource Slider}" />
			</Trigger>
		</Style.Triggers>
	</Style>

	<Style x:Key="{x:Type lvc:DefaultTooltip}"
	       TargetType="{x:Type lvc:DefaultTooltip}">
		<Setter Property="Background" Value="{DynamicResource Gray@300}" />
		<Setter Property="Foreground" Value="{DynamicResource White@50}" />
		<Setter Property="FontFamily" Value="{DynamicResource Font.Default}" />
		<Setter Property="BorderBrush" Value="{DynamicResource Brushes.HunterPie.Accent}" />
		<Setter Property="BulletSize" Value="4" />
		<Setter Property="BorderThickness" Value="1" />
	</Style>

	<Style TargetType="{x:Type TabItem}">
		<Setter Property="Background" Value="#171717" />
		<Setter Property="Foreground" Value="#FF9C9C9C" />
		<Setter Property="Opacity" Value="0.6" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type TabItem}">
					<Border x:Name="PART_Border">
						<ContentPresenter ContentSource="Header" />
					</Border>
					<ControlTemplate.Triggers>
						<Trigger Property="IsSelected" Value="True">
							<Setter Property="Opacity" Value="1" />
							<Setter TargetName="PART_Border" Property="BorderBrush" Value="Black" />
							<Setter Property="Foreground" Value="{DynamicResource Brushes.HunterPie.Accent}" />
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style TargetType="{x:Type TextBlock}">
		<Setter Property="FontFamily" Value="{DynamicResource Font.Default}" />
	</Style>

	<DataTemplate x:Key="DATA_TEMPLATE_SETTINGS_ENUM_ELEMENT">
		<TextBlock FontFamily="{DynamicResource Font.Default}"
		           Text="{Binding ., Mode=OneTime, Converter={StaticResource EnumToStringConverter}}" />
	</DataTemplate>

	<Style TargetType="{x:Type ListBox}">
		<Setter Property="ItemContainerStyle">
			<Setter.Value>
				<Style TargetType="ListBoxItem">
					<Setter Property="Template">
						<Setter.Value>
							<ControlTemplate TargetType="{x:Type ListBoxItem}">
								<ContentPresenter />
							</ControlTemplate>
						</Setter.Value>
					</Setter>
				</Style>
			</Setter.Value>
		</Setter>
	</Style>

	<converters:PercentageToRelativeSizeConverter x:Key="PercentageToRelativeSizeConverter" />
	<converters:NoRightStrokeConverter x:Key="NoRightStrokeConverter" />
	<converters:EqualityToBooleanConverter x:Key="EqualityToBooleanConverter" />

	<Style x:Key="DefaultProgressBarStyle"
	       TargetType="{x:Type progress:Bar}">
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate>
					<Grid DataContext="{Binding ., RelativeSource={RelativeSource AncestorType={x:Type progress:Bar}}}">
						<!--  Background bar  -->
						<Border x:Name="PART_Background"
						        BorderBrush="{Binding BackgroundBorder, Mode=OneWay}"
						        BorderThickness="{Binding Border, Mode=OneWay}"
						        Background="{Binding BackgroundColor, Mode=OneWay}" />

						<!--  Foreground Bar  -->
						<Border BorderBrush="{Binding ForegroundBorder, Mode=OneWay}"
						        HorizontalAlignment="Left"
						        Background="{Binding ForegroundColor, Mode=OneWay}">
							<Border.Width>
								<MultiBinding Converter="{StaticResource PercentageToRelativeSizeConverter}">
									<Binding Mode="OneWay"
									         Path="Value" />
									<Binding Mode="OneWay"
									         Path="MaxValue" />
									<Binding ElementName="PART_Background"
									         Mode="OneWay"
									         Path="ActualWidth" />
								</MultiBinding>
							</Border.Width>

							<Border.Style>
								<Style TargetType="Border">
									<Style.Triggers>
										<DataTrigger Value="True">
											<DataTrigger.Binding>
												<MultiBinding Converter="{StaticResource EqualityToBooleanConverter}">
													<Binding Mode="OneWay"
													         Path="Value" />
													<Binding Mode="OneWay"
													         Path="MaxValue" />
												</MultiBinding>
											</DataTrigger.Binding>
											<Setter Property="BorderThickness" Value="{Binding Border, Mode=OneWay}" />
										</DataTrigger>

										<DataTrigger Value="False">
											<DataTrigger.Binding>
												<MultiBinding Converter="{StaticResource EqualityToBooleanConverter}">
													<Binding Mode="OneWay"
													         Path="Value" />
													<Binding Mode="OneWay"
													         Path="MaxValue" />
												</MultiBinding>
											</DataTrigger.Binding>
											<Setter Property="BorderThickness" Value="{Binding Border, Mode=OneWay, Converter={StaticResource NoRightStrokeConverter}}" />
										</DataTrigger>
									</Style.Triggers>
								</Style>
							</Border.Style>
						</Border>

						<ContentControl Content="{Binding Content, Mode=OneWay}" />
					</Grid>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style x:Key="ExpanderNoHeaderStyle"
	       TargetType="Expander">
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="Expander">
					<Border BorderBrush="{TemplateBinding BorderBrush}"
					        BorderThickness="{TemplateBinding BorderThickness}"
					        Background="{TemplateBinding Background}">
						<ContentPresenter x:Name="PART_Presenter" />
					</Border>

					<ControlTemplate.Triggers>
						<Trigger Property="IsExpanded" Value="True">
							<Setter TargetName="PART_Presenter" Property="Visibility" Value="Visible" />
						</Trigger>

						<Trigger Property="IsExpanded" Value="False">
							<Setter TargetName="PART_Presenter" Property="Visibility" Value="Collapsed" />
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style BasedOn="{StaticResource DefaultProgressBarStyle}"
	       TargetType="{x:Type progress:Bar}" />

	<Style TargetType="{x:Type label:Tag}">
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type label:Tag}">
					<Border BorderBrush="{TemplateBinding Foreground}"
					        Padding="4,4"
					        Background="{TemplateBinding Background}"
					        CornerRadius="5">
						<TextBlock FontFamily="{DynamicResource Font.Default}"
						           FontSize="13"
						           Foreground="{TemplateBinding Foreground}"
						           Text="{TemplateBinding Label}" />
					</Border>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>
</ResourceDictionary>