<UserControl x:Class="HunterPie.UI.Overlay.Views.WidgetHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HunterPie.UI.Overlay.Views"
             xmlns:buttons="clr-namespace:HunterPie.UI.Controls.Buttons"
             xmlns:converters="clr-namespace:HunterPie.UI.Architecture.Converters"
             xmlns:image="clr-namespace:HunterPie.UI.Controls.Image"
             xmlns:viewModels="clr-namespace:HunterPie.UI.Overlay.ViewModels"
             mc:Ignorable="d"
             Height="30"
             Loaded="OnLoaded"
             d:DataContext="{d:DesignInstance viewModels:WidgetContext}">
    <UserControl.Resources>
        <converters:RelativeSizeConverter x:Key="RelativeSizeConverter"/>
    </UserControl.Resources>
    <Border BorderThickness="1"
            BorderBrush="{DynamicResource Gray}"
            Background="{DynamicResource Gray@100}"
            CornerRadius="5,5,0,0">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="30"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <image:Icon Grid.Column="0"
                        Image="{StaticResource ICON_HUNTERPIE}"
                        Foreground="{DynamicResource Gray}"
                        Margin="3"/>

            <TextBlock Grid.Column="1"
                       Text="{Binding ViewModel.Title, Mode=OneWay}"
                       d:Text="Widget Name"
                       Foreground="{DynamicResource White@500}"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Left"
                       FontWeight="Medium"/>

            <StackPanel Grid.Column="2" 
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <TextBlock VerticalAlignment="Center"
                           Foreground="{DynamicResource White@500}"
                           Margin="0,0,5,0">
                    <Run Text="x:"/>
                    <Run d:Text="1000"
                         Text="{Binding ViewModel.Settings.Position.X, Mode=OneWay, StringFormat=\{0:F0\}}"
                         Foreground="{DynamicResource White@100}"/>
                    <Run Text="y:"/>
                    <Run d:Text="800"
                         Text="{Binding ViewModel.Settings.Position.Y, Mode=OneWay, StringFormat=\{0:F0\}}"
                         Foreground="{DynamicResource White@100}"/>
                </TextBlock>
                <buttons:Button Width="20"
                                HorizontalContentAlignment="Stretch"
                                VerticalContentAlignment="Stretch"
                                Margin="2,0"
                                Click="OnHideButtonClick">
                    <image:Icon d:Image="{StaticResource ICON_EYE}"
                                d:Foreground="{StaticResource Green@100}">
                        <image:Icon.Style>
                            <Style TargetType="image:Icon">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ViewModel.Settings.Enabled.Value, Mode=OneWay}"
                                                 Value="False">
                                        <Setter Property="Image" Value="{StaticResource ICON_EYE_INVISIBLE}"/>
                                        <Setter Property="Foreground" Value="{DynamicResource Red@100}"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding ViewModel.Settings.Enabled.Value, Mode=OneWay}"
                                                 Value="True">
                                        <Setter Property="Image" Value="{StaticResource ICON_EYE}"/>
                                        <Setter Property="Foreground" Value="{DynamicResource Green@100}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </image:Icon.Style>
                    </image:Icon>
                </buttons:Button>
                <buttons:Button Width="20"
                                HorizontalContentAlignment="Stretch"
                                VerticalContentAlignment="Stretch"
                                Margin="2,0"
                                Click="OnCloseButtonClick">
                    <image:Icon Image="{StaticResource ICON_TRASH}"
                                Foreground="{DynamicResource Red@100}"/>
                </buttons:Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
