<Window x:Class="HunterPie.UI.Overlay.Components.WidgetBase"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HunterPie.UI.Overlay.Components"
        mc:Ignorable="d"
        Title="WidgetBase" 
        WindowStyle="None"
        AllowsTransparency="True"
        Background="#01000000"
        Width="400"
        Height="250">

    <Grid>
        <!-- Widget content -->
        <Grid Margin="10,50,10,10">
            <ContentControl Content="{Binding Widget, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:WidgetBase}}}"/>
        </Grid>

        <Grid x:Name="PART_Base"
              Opacity="0"
              d:Opacity="1">
            <local:WidgetHeader Height="30"
                            Margin="20,0,20,0"
                            VerticalAlignment="Top"/>
            <Border Margin="0,40,0,0"
                x:Name="PART_Highlight"
                BorderThickness="2" 
                Background="{StaticResource HUNTERPIE_ACCENT_COLOR_TRANSPARENT}" 
                BorderBrush="{StaticResource HUNTERPIE_ACCENT_COLOR}"/>
            <Grid.Style>
                <Style TargetType="{x:Type Grid}">
                    <Style.Triggers>
                        <!-- OnMouseOver -->
                        <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}" Value="True">
                            <DataTrigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard Storyboard.TargetProperty="Opacity">
                                        <DoubleAnimation Duration="0:0:0.15"
                                                 To="1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.EnterActions>
                            <DataTrigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard Storyboard.TargetProperty="Opacity">
                                        <DoubleAnimation Duration="0:0:0.15"
                                                 To="0"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.ExitActions>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
        </Grid>
    </Grid>
</Window>
