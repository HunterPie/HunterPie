<UserControl x:Class="HunterPie.UI.Overlay.Widgets.Activities.View.HarvestFertilizerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HunterPie.UI.Overlay.Widgets.Activities.View"
             xmlns:vm="clr-namespace:HunterPie.UI.Overlay.Widgets.Activities.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="30" 
             d:DesignWidth="270"
             d:DataContext="{d:DesignInstance vm:HarvestFertilizerViewModel}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="30"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Border Grid.ColumnSpan="2"
                Grid.Column="0"
                BorderThickness="1"
                BorderBrush="{StaticResource GRAY_300}"/>
        <!-- Background bar -->
        <Grid Grid.Column="1"
              HorizontalAlignment="Left"
              d:Width="200"
              MaxWidth="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType={x:Type Grid}}}"
              Margin="1,1,0,1"
              ClipToBounds="True">
            <Grid.Width>
                <MultiBinding Converter="{StaticResource CurrentValueToWidthConverter}">
                    <Binding Path="DataContext.Amount" RelativeSource="{RelativeSource AncestorType=local:CohootNestView}" FallbackValue="0"/>
                    <Binding Path="DataContext.MaxAmount" RelativeSource="{RelativeSource AncestorType=local:CohootNestView}" FallbackValue="1"/>
                    <Binding Path="MaxWidth" RelativeSource="{RelativeSource Self}"/>
                    <Binding Path="Margin" RelativeSource="{RelativeSource Self}"/>
                </MultiBinding>
            </Grid.Width>
            <Rectangle>
                <Rectangle.Fill>
                    <LinearGradientBrush EndPoint="1,0.5" StartPoint="0.5,1">
                        <GradientStop Color="#3361FFC9" Offset="0"/>
                        <GradientStop Color="#3300F9FF" Offset="1"/>
                    </LinearGradientBrush>
                </Rectangle.Fill>
            </Rectangle>
            <Rectangle Fill="#10FFFFFF" 
                       Width="30" 
                       RenderTransformOrigin="0.5,0.5"
                       HorizontalAlignment="Left" 
                       Margin="0,0,0,0">
                <Rectangle.Effect>
                    <BlurEffect Radius="20" 
                                KernelType="Gaussian"/>
                </Rectangle.Effect>
                <Rectangle.Style>
                    <Style TargetType="{x:Type Rectangle}">
                        <Style.Triggers>
                            <Trigger Property="IsVisible"
                                     Value="True">
                                <Trigger.EnterActions>

                                    <BeginStoryboard>
                                        <Storyboard RepeatBehavior="Forever"
                                                    Storyboard.TargetProperty="Margin">
                                            <ThicknessAnimation From="-40,0,0,0"
                                                                To="300,0,0,0"
                                                                Duration="0:0:3"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Rectangle.Style>
            </Rectangle>
        </Grid>

        <Rectangle Grid.Column="0"
                   Fill="{StaticResource GRAY_300}"/>

        <Image Source="{StaticResource ICON_FERTILIZER_GROWTH_S}"
               Grid.Column="0"
               Margin="3"/>

        <TextBlock Grid.Column="1"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Left"
                   Text="Fertilizer name"
                   Margin="2"
                   Foreground="{StaticResource WHITE_100}"
                   FontSize="14"
                   FontWeight="Medium"/>

        <TextBlock Grid.Column="1"
                   VerticalAlignment="Center"
                   Text="x4"
                   HorizontalAlignment="Right"
                   Margin="2"
                   FontSize="14"
                   Foreground="{StaticResource WHITE_100}"
                   FontWeight="Light"/>
    </Grid>
</UserControl>
