<UserControl x:Class="HunterPie.UI.Overlay.Widgets.Activities.Wilds.Views.MHWildsActivitiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HunterPie.UI.Overlay.Widgets.Activities.Wilds.Views"
             xmlns:viewModels="clr-namespace:HunterPie.UI.Overlay.Widgets.Activities.Wilds.ViewModels"
             mc:Ignorable="d"
             Width="230"
             d:DataContext="{d:DesignInstance viewModels:MHWildsActivitiesViewModel}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <local:MaterialRetrievalView Grid.Row="0" 
                                     DataContext="{Binding MaterialRetrieval, Mode=OneTime}"
                                     Visibility="{Binding DataContext.IsMaterialRetrievalEnabled.Value, RelativeSource={RelativeSource AncestorType={x:Type local:MHWildsActivitiesView}}, Converter={StaticResource BooleanToVisibilityConverter}}"/>

        <UniformGrid Grid.Row="1"
                     Columns="2">
            <local:SupportShipView Height="38"
                                   Margin="2"
                                   DataContext="{Binding SupportShip, Mode=OneTime}"
                                   Visibility="{Binding DataContext.IsSupportShipEnabled.Value, RelativeSource={RelativeSource AncestorType={x:Type local:MHWildsActivitiesView}}, Converter={StaticResource BooleanToVisibilityConverter}}"/>

            <local:IngredientsCenterView Height="38"
                                         Margin="2"
                                         DataContext="{Binding IngredientsCenter, Mode=OneTime}"
                                         Visibility="{Binding DataContext.IsIngredientsCenterEnabled.Value, RelativeSource={RelativeSource AncestorType={x:Type local:MHWildsActivitiesView}}, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </UniformGrid>
        
    </Grid>
</UserControl>
