<UserControl x:Class="HunterPie.UI.Overlay.Widgets.Activities.Rise.Views.MeowcenariesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:HunterPie.UI.Overlay.Widgets.Activities.Rise.ViewModels"
             xmlns:image="clr-namespace:HunterPie.UI.Controls.Image"
             xmlns:progress="clr-namespace:HunterPie.UI.Controls.Progress"
             xmlns:sys="clr-namespace:System;assembly=System.Runtime"
             xmlns:brushes="clr-namespace:HunterPie.UI.Architecture.Converters.Brushes"
             d:DataContext="{d:DesignInstance Type=viewModels:MeowcenariesViewModel}"
             mc:Ignorable="d" 
             d:Height="45" 
             d:Width="230">
    <UserControl.Resources>
        <brushes:BrushOpacityConverter x:Key="BrushOpacityConverter"/>
    </UserControl.Resources>
    <Border Background="{Binding Source={StaticResource Gray@100}, Converter={StaticResource BrushOpacityConverter}, ConverterParameter={StaticResource Widgets.Background.Opacity}}"
            BorderBrush="{StaticResource Gray}"
            BorderThickness="1"
            CornerRadius="5">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <image:Icon Grid.Column="0"
                        Image="{StaticResource ICON_DOJO}"
                        Foreground="{StaticResource White@200}"
                        Margin="5"/>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="2*"/>
                </Grid.RowDefinitions>

                <!-- Step progress -->
                <progress:Gauge Grid.Row="0"
                                Foreground="{StaticResource Green}"
                                Background="{StaticResource Gray@200}"
                                BorderBrush="{StaticResource Gray}"
                                Markers="{Binding MaxSteps, Mode=OneWay}"
                                d:Markers="5"
                                Height="5"
                                d:Current="3"
                                d:Max="5"
                                Current="{Binding Step, Mode=OneWay}"
                                Max="{Binding MaxSteps, Mode=OneWay}"
                                Margin="0,0,6,0"/>

                <!-- Buddy Count -->
                <Border Grid.Row="1"
                        Background="{StaticResource Gray@50}"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        Padding="10,2"
                        CornerRadius="5">
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="Buddies"
                                   FontSize="8"
                                   Foreground="{StaticResource White@200}"/>
                        <TextBlock Foreground="{StaticResource White}"
                                   HorizontalAlignment="Center">
                            <Run Text="{Binding BuddyCount, Mode=OneWay}"
                                 d:Text="3"/><!--
                         --><Run Text="/"/><!--
                         --><Run Text="{Binding MaxBuddyCount, Mode=OneWay}"
                                d:Text="5"/>
                        </TextBlock>
                    </StackPanel>
                </Border>

                <!-- Outcome -->
                <ItemsControl Grid.Row="1"
                              ItemsSource="{Binding Outcome, Mode=OneTime}"
                              d:ItemsSource="{d:SampleData ItemCount=5}"
                              HorizontalAlignment="Right"
                              Margin="5,0">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type sys:Boolean}">
                            <progress:Pill Width="9"
                                           BorderBrush="{StaticResource Gray}"
                                           Height="12"
                                           Margin="1,0">
                                <progress:Pill.Style>
                                    <Style TargetType="progress:Pill">
                                        <Setter Property="Foreground" Value="{StaticResource Gray@200}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ., Mode=OneTime}"
                                                         Value="True">
                                                <Setter Property="Foreground" Value="{StaticResource Yellow}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </progress:Pill.Style>
                            </progress:Pill>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </Grid>
    </Border>
</UserControl>
