<UserControl x:Class="HunterPie.UI.Overlay.Widgets.Activities.Rise.Views.CohootNestsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModels="clr-namespace:HunterPie.UI.Overlay.Widgets.Activities.Rise.ViewModels"
             xmlns:image="clr-namespace:HunterPie.UI.Controls.Image"
             mc:Ignorable="d" 
             d:DesignHeight="40" 
             d:DesignWidth="240"
             d:DataContext="{d:DesignInstance viewModels:CohootNestsViewModel}">
    <Border Background="{StaticResource GRAY_200}"
            BorderBrush="{StaticResource GRAY}"
            BorderThickness="1,0,1,1"
            CornerRadius="0,0,7,7">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <image:Icon Grid.Column="0"
                        Foreground="{StaticResource WHITE_200}"
                        Image="{StaticResource ICON_COHOOT}"
                        Margin="5"/>

            <UniformGrid Grid.Column="1"
                         Columns="2">
                
                <!-- Kamura -->
                <Border Background="{StaticResource GRAY_50}"
                        CornerRadius="5"
                        Margin="2">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="2*"/>
                            <RowDefinition Height="3*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0"
                                   Text="Kamura"
                                   FontWeight="Medium"
                                   Foreground="{StaticResource WHITE_200}"
                                   HorizontalAlignment="Center"/>

                        <ItemsControl Grid.Row="1">
                            
                        </ItemsControl>
                    </Grid>
                </Border>

                <!-- Elgado -->
                <Border Background="{StaticResource GRAY_50}"
                        CornerRadius="5"
                        Margin="2">
                    <Grid>

                    </Grid>
                </Border>
            </UniformGrid>
        </Grid>
    </Border>
</UserControl>