<UserControl x:Class="HunterPie.UI.Overlay.Widgets.Activities.Rise.Views.SubmarinesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:HunterPie.UI.Overlay.Widgets.Activities.Rise.ViewModels"
             xmlns:views="clr-namespace:HunterPie.UI.Overlay.Widgets.Activities.Rise.Views"
             xmlns:brushes="clr-namespace:HunterPie.UI.Architecture.Converters.Brushes"
             d:DataContext="{d:DesignInstance Type=viewModels:SubmarinesViewModel}"
             mc:Ignorable="d">
    <UserControl.Resources>
        <brushes:BrushOpacityConverter x:Key="BrushOpacityConverter"/>
    </UserControl.Resources>
    <Border BorderThickness="1,1,1,0"
            BorderBrush="{StaticResource Gray}"
            Background="{Binding Source={StaticResource Gray@100}, Converter={StaticResource BrushOpacityConverter}, ConverterParameter={StaticResource Widgets.Background.Opacity}}"
            CornerRadius="5,5,0,0">
        <ItemsControl ItemsSource="{Binding Submarines, Mode=OneWay}"
                      d:ItemsSource="{d:SampleData ItemCount=3}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Vertical"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <views:SubmarineView Height="35"
                                         BorderThickness="0,0,0,1"
                                         BorderBrush="{StaticResource Gray}">
                        <views:SubmarineView.Style>
                            <Style TargetType="views:SubmarineView">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsActive, Mode=OneWay}"
                                                 Value="True">
                                        <Setter Property="Opacity" Value="1"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsActive, Mode=OneWay}"
                                                 Value="False">
                                        <Setter Property="Opacity" Value="0.3"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </views:SubmarineView.Style>                        
                    </views:SubmarineView>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Border>
</UserControl>
