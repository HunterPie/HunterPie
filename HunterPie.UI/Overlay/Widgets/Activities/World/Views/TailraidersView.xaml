<UserControl x:Class="HunterPie.UI.Overlay.Widgets.Activities.World.Views.TailraidersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:HunterPie.UI.Architecture.Converters"
             xmlns:loc="clr-namespace:HunterPie.UI.Architecture.Assets"
             xmlns:viewModels="clr-namespace:HunterPie.UI.Overlay.Widgets.Activities.World.ViewModels"
             xmlns:brushes="clr-namespace:HunterPie.UI.Architecture.Converters.Brushes"
             xmlns:image="clr-namespace:HunterPie.UI.Controls.Image"
             mc:Ignorable="d" 
             d:DesignHeight="40" 
             d:DesignWidth="135"
             d:Background="#DF202327"
             d:DataContext="{d:DesignInstance viewModels:TailraidersViewModel}">
    <UserControl.Resources>
        <brushes:BrushToColorConverter x:Key="BrushToColorConverter"/>
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter"/>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <Border BorderBrush="{StaticResource GRAY}"
            BorderThickness="1"
            CornerRadius="5"
            Background="{StaticResource GRAY_200}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <image:Icon Grid.Column="0"
                        Image="{StaticResource ICON_TAILRAIDERS}"
                        Foreground="{StaticResource WHITE_200}"
                        Margin="3"
                        Width="{Binding ActualHeight, RelativeSource={RelativeSource Self}}"/>

            
            <Ellipse Grid.Column="1"
                     Width="7"
                     Height="{Binding Width, RelativeSource={RelativeSource Self}}"
                     VerticalAlignment="Top"
                     HorizontalAlignment="Right"
                     d:Fill="{StaticResource GREEN}"
                     Margin="3">
                <Ellipse.Effect>
                    <DropShadowEffect Color="{Binding Fill, RelativeSource={RelativeSource AncestorType={x:Type Ellipse}}, Converter={StaticResource BrushToColorConverter}}"
                                      ShadowDepth="0"
                                      BlurRadius="10"/>
                </Ellipse.Effect>
                <Ellipse.Style>
                    <Style TargetType="Ellipse">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsDeployed, Mode=OneWay}" Value="True">
                                <Setter Property="Fill" Value="{StaticResource GREEN}"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsDeployed, Mode=OneWay}" Value="False">
                                <Setter Property="Fill" Value="{StaticResource RED}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Ellipse.Style>
            </Ellipse>

            <TextBlock Grid.Column="1"
                       Text="---"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"
                       FontSize="20"
                       Foreground="{StaticResource GRAY}"
                       Visibility="{Binding IsDeployed, Mode=OneWay, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>

            <TextBlock Grid.Column="1"
                       d:Text="5"
                       Text="{Binding QuestsLeft, Mode=OneWay}"
                       Foreground="{StaticResource WHITE_200}"
                       FontSize="16"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"
                       FontWeight="Medium"
                       Margin="0,0,0,10"
                       Visibility="{Binding IsDeployed, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            <TextBlock Grid.Column="1"
                       d:Text="days"
                       Text="{loc:LocalizationString '//Strings/Client/Overlay/String[@Id=&quot;TAILRAIDERS_QUEST_STRING&quot;]'}"
                       Foreground="{StaticResource WHITE_500}"
                       FontSize="14"
                       VerticalAlignment="Bottom"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,5"
                       Visibility="{Binding IsDeployed, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </Grid>
    </Border>
</UserControl>
