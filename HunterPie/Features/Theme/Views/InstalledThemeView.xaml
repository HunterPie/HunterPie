<UserControl x:Class="HunterPie.Features.Theme.Views.InstalledThemeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HunterPie.Features.Theme.Views"
             xmlns:image="clr-namespace:HunterPie.UI.Controls.Image;assembly=HunterPie.UI"
             xmlns:viewModels="clr-namespace:HunterPie.Features.Theme.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="60" 
             d:DesignWidth="800"
             d:DataContext="{d:DesignInstance viewModels:InstalledThemeViewModel}">
    <Border Background="{StaticResource Brushes.HunterPie.Background.Tertiary}"
            CornerRadius="5"
            BorderBrush="{StaticResource Brushes.HunterPie.Border}"
            BorderThickness="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <image:Icon Grid.Column="0"
                        Foreground="{StaticResource Brushes.HunterPie.Foreground.Tertiary}"
                        Background="{StaticResource Brushes.HunterPie.Background.Tertiary}"
                        Image="{StaticResource Icons.Chevron.UpDown}"
                        Margin="10"
                        MouseLeftButtonDown="OnDragButtonDown"
                        Cursor="SizeAll"/>

            <Border Grid.Column="1"
                    Background="{StaticResource Brushes.HunterPie.Background.Secondary}"
                    CornerRadius="5,4.5,4.5,5">
                <Grid Margin="5">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="1*"/>
                        <RowDefinition Height="2*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                               Text="{Binding Name, Mode=OneWay}"
                               d:Text="Theme Name"
                               FontSize="14"
                               FontWeight="Medium"
                               Foreground="{StaticResource Brushes.HunterPie.Foreground.Primary}"/>
                    <TextBlock Grid.Row="1"
                               Text="by Username"
                               FontSize="10"
                               Foreground="{StaticResource Brushes.HunterPie.Foreground.Tertiary}"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>
