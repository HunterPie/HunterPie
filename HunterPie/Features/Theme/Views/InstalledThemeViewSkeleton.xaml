<UserControl x:Class="HunterPie.Features.Theme.Views.InstalledThemeViewSkeleton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HunterPie.Features.Theme.Views"
             xmlns:loading="clr-namespace:HunterPie.UI.Controls.Loading;assembly=HunterPie.UI"
             mc:Ignorable="d" 
             d:DesignHeight="80" 
             d:DesignWidth="800">
    <Border Background="{DynamicResource Brushes.HunterPie.Background.Secondary}"
            CornerRadius="5"
            BorderBrush="{DynamicResource Brushes.HunterPie.Border}"
            BorderThickness="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" 
                  Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <loading:Skeleton Grid.Row="0" 
                                  Height="15"
                                  Width="100"
                                  HorizontalAlignment="Left"
                                  Background="{DynamicResource Brushes.HunterPie.Foreground.Primary}"
                                  Opacity="0.2"/>

                <loading:Skeleton Grid.Row="1" 
                                  Height="10"
                                  Margin="0,2"
                                  Width="50"
                                  HorizontalAlignment="Left"
                                  Background="{DynamicResource Brushes.HunterPie.Foreground.Secondary}"
                                  Opacity="0.2"/>

                <loading:Skeleton Grid.Row="2"
                                  Margin="0,2"
                                  Background="{DynamicResource Brushes.HunterPie.Foreground.Tertiary}"
                                  Opacity="0.10"/>
            </Grid>
            <StackPanel Grid.Column="1" 
                        Orientation="Horizontal">
                <loading:Skeleton Height="30"
                                  Width="30"
                                  Margin="5"
                                  HorizontalAlignment="Left"
                                  Background="{DynamicResource Brushes.HunterPie.Foreground.Tertiary}"
                                  Opacity="0.3"/>
                <loading:Skeleton Height="30"
                                  Width="30"
                                  Margin="5"
                                  HorizontalAlignment="Left"
                                  Background="{DynamicResource Brushes.HunterPie.Foreground.Secondary}"
                                  Opacity="0.3"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
