<UserControl x:Class="HunterPie.Features.Theme.Views.ThemeHomeTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HunterPie.Features.Theme.Views"
             xmlns:viewModels="clr-namespace:HunterPie.Features.Theme.ViewModels"
             xmlns:viewer="clr-namespace:HunterPie.UI.Controls.Viewer;assembly=HunterPie.UI"
             mc:Ignorable="d" 
             d:DesignHeight="450" 
             d:DesignWidth="917"
             d:DataContext="{d:DesignInstance viewModels:ThemeHomeTabViewModel}">
    <Grid Margin="5">
        <ItemsControl ItemsSource="{Binding Themes, Mode=OneTime}"
                      d:ItemsSource="{d:SampleData ItemCount=10}">
            <ItemsControl.Template>
                <ControlTemplate>
                    <viewer:SmoothScrollViewer VerticalScrollBarVisibility="Auto">
                        <viewer:AutoWrapPanel IsItemsHost="True"
                                              MinItemWidth="300"
                                              ItemHeight="250">
                        </viewer:AutoWrapPanel>
                    </viewer:SmoothScrollViewer>
                </ControlTemplate>
            </ItemsControl.Template>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <local:ThemeCardView Margin="2"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
