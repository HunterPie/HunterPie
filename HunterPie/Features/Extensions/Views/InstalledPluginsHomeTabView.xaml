<UserControl x:Class="HunterPie.Features.Extensions.Views.InstalledPluginsHomeTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:buttons="clr-namespace:HunterPie.UI.Controls.Buttons;assembly=HunterPie.UI"
             xmlns:converters="clr-namespace:HunterPie.UI.Architecture.Converters;assembly=HunterPie.UI"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:display="clr-namespace:HunterPie.UI.Controls.Display;assembly=HunterPie.UI"
             xmlns:image="clr-namespace:HunterPie.UI.Controls.Image;assembly=HunterPie.UI"
             xmlns:loading="clr-namespace:HunterPie.UI.Controls.Loading;assembly=HunterPie.UI"
             xmlns:local="clr-namespace:HunterPie.Features.Extensions.Views"
             xmlns:markup="clr-namespace:HunterPie.UI.Architecture.Markup;assembly=HunterPie.UI"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:HunterPie.Features.Extensions.ViewModels"
             xmlns:viewer="clr-namespace:HunterPie.UI.Controls.Viewer;assembly=HunterPie.UI"
             d:DataContext="{d:DesignInstance viewModels:InstalledPluginsHomeTabViewModel}"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">
	<UserControl.Resources>
		<converters:ConverterPipeline x:Key="InverseNumberToVisibilityConverter">
			<converters:NumberToBooleanConverter />
			<converters:InverseBooleanToVisibilityConverter />
		</converters:ConverterPipeline>
	</UserControl.Resources>
	<Grid Margin="5">
		<Grid.RowDefinitions>
			<RowDefinition Height="40" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<Grid Grid.Row="0">
			<buttons:Button BorderBrush="{DynamicResource Brushes.HunterPie.Border}"
			                BorderThickness="1"
			                Margin="2"
			                Padding="5,0"
			                HorizontalAlignment="Left"
			                HorizontalContentAlignment="Stretch"
			                VerticalContentAlignment="Stretch"
			                Background="{DynamicResource Brushes.HunterPie.Background.Primary}"
			                Click="OnOpenFolderButtonClicked"
			                CornerRadius="5">
				<Grid>
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="20" />
						<ColumnDefinition Width="*" />
					</Grid.ColumnDefinitions>

					<image:Icon Grid.Column="0"
					            Margin="5"
					            Foreground="{DynamicResource Brushes.HunterPie.Foreground.Primary}"
					            Image="{StaticResource Icons.Folder}" />

					<TextBlock Grid.Column="1"
					           VerticalAlignment="Center"
					           Foreground="{DynamicResource Brushes.HunterPie.Foreground.Primary}"
					           Text="{markup:Localization '//Strings/Client/Generics/Generic[@Id=&quot;OPEN_FOLDER&quot;]'}" />
				</Grid>
			</buttons:Button>
			<buttons:Button BorderBrush="{DynamicResource Brushes.HunterPie.Border}"
			                BorderThickness="1"
			                Margin="2"
			                Padding="5,0"
			                HorizontalAlignment="Right"
			                HorizontalContentAlignment="Stretch"
			                VerticalContentAlignment="Stretch"
			                Background="{DynamicResource Brushes.HunterPie.Background.Primary}"
			                Click="OnRefreshButtonClicked"
			                CornerRadius="5">
				<Grid>
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="20" />
						<ColumnDefinition Width="*" />
					</Grid.ColumnDefinitions>

					<image:Icon Grid.Column="0"
					            Margin="5"
					            Foreground="{DynamicResource Brushes.HunterPie.Foreground.Primary}"
					            Image="{StaticResource Icons.Refresh}" />

					<TextBlock Grid.Column="1"
					           VerticalAlignment="Center"
					           Foreground="{DynamicResource Brushes.HunterPie.Foreground.Primary}"
					           Text="{markup:Localization '//Strings/Client/Generics/Generic[@Id=&quot;REFRESH_STRING&quot;]'}" />
				</Grid>
			</buttons:Button>
		</Grid>

		<display:Conditional Grid.Row="1"
		                     d:Condition="True"
		                     Condition="{Binding IsRefreshing, Mode=OneWay}">
			<!--  Loading skeleton  -->
			<display:Conditional.Component>
				<DataTemplate>
					<ItemsControl ItemsSource="{markup:CreateCollection ItemsCount=10}">
						<ItemsControl.ItemsPanel>
							<ItemsPanelTemplate>
								<StackPanel CanVerticallyScroll="False"
								            IsItemsHost="True"
								            Orientation="Vertical" />
							</ItemsPanelTemplate>
						</ItemsControl.ItemsPanel>

						<ItemsControl.ItemTemplate>
							<DataTemplate>
								<local:InstalledThemeViewSkeleton Height="80"
								                                  Margin="2" />
							</DataTemplate>
						</ItemsControl.ItemTemplate>
					</ItemsControl>
				</DataTemplate>
			</display:Conditional.Component>

			<!--  Plugins  -->
			<display:Conditional.Otherwise>
				<DataTemplate>
					<Grid>
						<Border BorderBrush="{DynamicResource Brushes.HunterPie.Border}"
						        BorderThickness="1"
						        Padding="10"
						        Background="{DynamicResource Brushes.HunterPie.Background.Secondary}"
						        CornerRadius="5"
						        Visibility="{Binding Plugins.Count, Converter={StaticResource InverseNumberToVisibilityConverter}}">
							<StackPanel>
								<image:Icon Height="250"
								            Foreground="{DynamicResource Brushes.HunterPie.Foreground.Secondary}"
								            Image="{StaticResource Visuals.ThemeBoy}"
								            Opacity="0.5" />
								<TextBlock HorizontalAlignment="Center"
								           FontSize="14"
								           Foreground="{DynamicResource Brushes.HunterPie.Foreground.Secondary}"
								           Text="{markup:Localization '//Strings/Client/Generics/Generic[@Id=&quot;NO_PLUGINS_FOUND_STRING&quot;]'}"
								           TextAlignment="Center" />
							</StackPanel>
						</Border>

						<ItemsControl d:ItemsSource="{d:SampleData ItemCount=10}"
						              ItemsSource="{Binding Plugins, Mode=OneTime}">
							<ItemsControl.Template>
								<ControlTemplate>
									<viewer:SmoothScrollViewer VerticalScrollBarVisibility="Auto">
										<StackPanel IsItemsHost="True" />
									</viewer:SmoothScrollViewer>
								</ControlTemplate>
							</ItemsControl.Template>
							<ItemsControl.ItemTemplate>
								<DataTemplate>
									<local:InstalledPluginView Height="80"
									                           Margin="2" />
								</DataTemplate>
							</ItemsControl.ItemTemplate>
						</ItemsControl>
					</Grid>
				</DataTemplate>
			</display:Conditional.Otherwise>
		</display:Conditional>
	</Grid>
</UserControl>
