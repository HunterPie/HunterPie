<UserControl x:Class="HunterPie.Features.Extensions.Views.InstalledThemeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:buttons="clr-namespace:HunterPie.UI.Controls.Buttons;assembly=HunterPie.UI"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:effects="clr-namespace:HunterPie.UI.Controls.Effects;assembly=HunterPie.UI"
             xmlns:image="clr-namespace:HunterPie.UI.Controls.Image;assembly=HunterPie.UI"
             xmlns:label="clr-namespace:HunterPie.UI.Controls.Label;assembly=HunterPie.UI"
             xmlns:local="clr-namespace:HunterPie.Features.Extensions.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:HunterPie.Features.Extensions.ViewModels"
             d:DataContext="{d:DesignInstance viewModels:InstalledThemeViewModel}"
             d:DesignHeight="80"
             d:DesignWidth="800"
             mc:Ignorable="d">
	<UserControl.Resources>
		<DataTemplate x:Key="Internal.DefaultTemplate">
			<Border BorderBrush="{DynamicResource Brushes.HunterPie.Border}"
			        BorderThickness="1"
			        Background="{DynamicResource Brushes.HunterPie.Border}"
			        CornerRadius="5">
				<Grid>
					<Grid.ColumnDefinitions>
						<ColumnDefinition>
							<ColumnDefinition.Style>
								<Style TargetType="ColumnDefinition">
									<Setter Property="Width" Value="40" />
									<Style.Triggers>
										<DataTrigger Binding="{Binding IsEnabled, Mode=OneWay}"
										             Value="False">
											<Setter Property="Width" Value="0" />
										</DataTrigger>
									</Style.Triggers>
								</Style>
							</ColumnDefinition.Style>
						</ColumnDefinition>
						<ColumnDefinition Width="*" />
					</Grid.ColumnDefinitions>

					<effects:CursorHighLight Grid.Column="0">
						<image:Icon Margin="10"
						            Background="{DynamicResource Brushes.HunterPie.Border}"
						            Cursor="SizeAll"
						            Foreground="{DynamicResource Brushes.HunterPie.Foreground.Tertiary}"
						            Image="{DynamicResource Icons.Chevron.UpDown}"
						            MouseLeftButtonDown="OnDragButtonDown" />
					</effects:CursorHighLight>

					<Border Grid.Column="1"
					        Background="{DynamicResource Brushes.HunterPie.Background.Secondary}"
					        CornerRadius="5,4.5,4.5,5">
						<Grid>
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="*" />
								<ColumnDefinition Width="Auto" />
							</Grid.ColumnDefinitions>

							<Grid Grid.Column="0"
							      Margin="5">
								<Grid.RowDefinitions>
									<RowDefinition Height="Auto" />
									<RowDefinition Height="Auto" />
									<RowDefinition Height="*" />
								</Grid.RowDefinitions>

								<!--  Header  -->
								<StackPanel Grid.Row="0"
								            Orientation="Horizontal">
									<TextBlock Margin="0,0,5,0"
									           VerticalAlignment="Center"
									           d:Text="Name"
									           FontSize="14"
									           FontWeight="Medium"
									           Foreground="{DynamicResource Brushes.HunterPie.Foreground.Primary}"
									           Text="{Binding Name, Mode=OneWay}" />
									<label:Tag d:Label="v1.0.0"
									           Background="{DynamicResource Blue@700}"
									           FontSize="11"
									           Foreground="{DynamicResource Blue}"
									           Label="{Binding Version, Mode=OneWay, StringFormat={}v{0}}" />
								</StackPanel>


								<!--  Author  -->
								<TextBlock Grid.Row="1"
								           FontSize="10"
								           Foreground="{DynamicResource Brushes.HunterPie.Foreground.Tertiary}">
									<Run Text="by" />
									<Run d:Text="Haato"
									     Text="{Binding Author, Mode=OneWay}" />
								</TextBlock>

								<!--  Description  -->
								<TextBlock Grid.Row="2"
								           d:Text="Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed ac erat quis nisi lacinia lacinia in at lacus. Vivamus nec diam felis. Mauris hendrerit faucibus maximus."
								           Foreground="{DynamicResource Brushes.HunterPie.Foreground.Secondary}"
								           Text="{Binding Description, Mode=OneWay}"
								           TextTrimming="WordEllipsis"
								           TextWrapping="WrapWithOverflow" />
							</Grid>

							<StackPanel Grid.Column="1"
							            Margin="5"
							            Orientation="Horizontal">
								<buttons:Button BorderBrush="{DynamicResource Brushes.HunterPie.Border}"
								                BorderThickness="1"
								                Width="30"
								                Height="{Binding ActualWidth, RelativeSource={RelativeSource Self}}"
								                Margin="2.5"
								                HorizontalContentAlignment="Stretch"
								                VerticalContentAlignment="Stretch"
								                Background="{DynamicResource Brushes.HunterPie.Background.Primary}"
								                Click="OnOpenFolderClick"
								                CornerRadius="5">
									<image:Icon Foreground="{DynamicResource Brushes.HunterPie.Foreground.Secondary}"
									            Image="{DynamicResource Icons.Folder}" />
								</buttons:Button>

								<buttons:Switch Click="OnEnableTheme"
								                IsActive="{Binding IsEnabled, Mode=OneWay}" />
							</StackPanel>
						</Grid>
					</Border>
				</Grid>
				<Border.Style>
					<Style TargetType="Border">
						<Setter Property="Opacity" Value="1" />
						<Style.Triggers>
							<DataTrigger Binding="{Binding IsBeingDragged, Mode=OneWay}"
							             Value="True">
								<Setter Property="Opacity" Value="0.5" />
							</DataTrigger>
						</Style.Triggers>
					</Style>
				</Border.Style>
			</Border>
		</DataTemplate>

		<DataTemplate x:Key="Internal.DragOverTemplate">
			<Border BorderThickness="1"
			        Background="{DynamicResource Brushes.HunterPie.Background.Primary}"
			        CornerRadius="5"
			        RenderTransformOrigin="0.5,0.5">
				<Border.BorderBrush>
					<DrawingBrush TileMode="Tile"
					              Viewport="0,0,15,15"
					              ViewportUnits="Absolute">
						<DrawingBrush.Drawing>
							<DrawingGroup>
								<GeometryDrawing Brush="{DynamicResource Brushes.HunterPie.Border}">
									<GeometryDrawing.Geometry>
										<GeometryGroup>
											<RectangleGeometry Rect="0,0,1,1" />
											<RectangleGeometry Rect="1,1,1,1" />
										</GeometryGroup>
									</GeometryDrawing.Geometry>
								</GeometryDrawing>
							</DrawingGroup>
						</DrawingBrush.Drawing>
					</DrawingBrush>
				</Border.BorderBrush>

				<Border.Style>
					<Style TargetType="Border">
						<Style.Triggers>
							<Trigger Property="IsVisible" Value="True">
								<Trigger.EnterActions>
									<BeginStoryboard>
										<Storyboard>
											<DoubleAnimation Storyboard.TargetProperty="Opacity"
											                 From="0"
											                 Duration="0:0:0.250">
												<DoubleAnimation.EasingFunction>
													<QuadraticEase />
												</DoubleAnimation.EasingFunction>
											</DoubleAnimation>
										</Storyboard>
									</BeginStoryboard>
								</Trigger.EnterActions>
							</Trigger>
						</Style.Triggers>
					</Style>
				</Border.Style>
			</Border>
		</DataTemplate>
	</UserControl.Resources>

	<ContentPresenter d:ContentTemplate="{StaticResource Internal.DefaultTemplate}"
	                  Content="{Binding}">
		<ContentPresenter.Style>
			<Style TargetType="ContentPresenter">
				<Setter Property="ContentTemplate" Value="{StaticResource Internal.DefaultTemplate}" />
				<Style.Triggers>
					<MultiDataTrigger>
						<MultiDataTrigger.Conditions>
							<Condition Binding="{Binding IsEnabled, Mode=OneWay}" Value="True" />
							<Condition Binding="{Binding IsDraggingOver, Mode=OneWay}" Value="True" />
							<Condition Binding="{Binding IsBeingDragged, Mode=OneWay}" Value="False" />
						</MultiDataTrigger.Conditions>
						<Setter Property="ContentTemplate" Value="{StaticResource Internal.DragOverTemplate}" />
					</MultiDataTrigger>
				</Style.Triggers>
			</Style>
		</ContentPresenter.Style>
	</ContentPresenter>
</UserControl>
